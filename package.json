{
  "name": "bflabs-ui",
  "version": "0.1.0",
  "private": true,
  "description": "The shared BF Labs design system and React component library.",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "pnpm --filter @bflabs/ui build && pnpm --filter @bflabs/docs dev",
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "check": "pnpm typecheck && pnpm test && pnpm build"
  }
}

